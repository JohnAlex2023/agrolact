# Servidor
PORT=3000
NODE_ENV=development

# Frontend (CORS)
FRONTEND_URL=http://localhost:5173

# Base de datos (se configura al conectar Neon)
DATABASE_URL=

# Autenticación (se configura al implementar JWT)
JWT_SECRET=
JWT_EXPIRES_IN=8h